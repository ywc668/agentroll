# Superseded by ci.yml — kept as a no-op redirect to avoid breaking
# any branch protection rules that reference this workflow name.
# You can safely delete this file once branch protection rules are updated.
name: Lint (legacy — see ci.yml)

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  redirect:
    name: Run on Ubuntu
    runs-on: ubuntu-latest
    steps:
      - run: echo "Linting now runs in ci.yml (Lint job). This job is a no-op."
